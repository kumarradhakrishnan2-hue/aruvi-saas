// DEPRECATED — the allocation report is no longer rendered on the page.
// Per product spec, the final allocation step only offers PDF/Word downloads
// (see the `.reports-inline` / `.filebtn` block in Allocate.jsx). This component
// is kept as an empty stub only because the file could not be deleted from this
// workspace; nothing imports it. Safe to remove.
export default function AllocationReportView() {
  return null;
}
