# CHANGELOG — Lesson Plan Constitution · Mathematics · Secondary Stage

The `VERSION` line lives in the constitution; the history lives here (testing.md §3, P4).

---

## v1.3 — 2026-08-09 · three limits corrected against live generation (S4 · C3)

All three were breached by ch 4's live output, and in each case the evidence pointed at the
RULE rather than the plan. Nothing pedagogical changed; no prohibition was removed.

- **Rule 5 P1 — the consecutive-method cap gains an exception** (ARV-D-072). The cap on
  repeating a method across more than two consecutive periods was breached by all three of
  ch 4's canonicals, always at the chapter TAIL, where the sections genuinely converge on
  problem work (simplify rational expressions → solve applied area problems → synthesise).
  Satisfying the cap there means labelling a unit with a method its content does not support.
  The cap now yields where the anchored sections call for it; the Rule 5 weighting still
  binds and a run produced for convenience is still forbidden.
- **A3 `activity_title` — 10–13 words → 6–13** (ARV-D-076). 31 of ch 4's 36 units fell
  outside the band, every one of them SHORT. The titles are good at 6–9 words ("Reversing
  the Identity: Factorising Perfect-Square Trinomials") and padding them to reach ten makes
  them worse. A minimum missed in one direction by three independent generations is a
  minimum set too high.
- **Rule 6 `section_context` — 10–12 words → 6–12** (ARV-D-030's under-length half). Same
  argument, same direction. The UPPER bound is untouched and is the one the rule actually
  guards: "it is a label, not content." Over-length labels remain defects and ch 4's three
  were repaired rather than excused.

Artefacts: `genon/out/stage_prep_mathematics_secondary/lesson_plan_constitution_v1.2_pre.txt`;
the evidence is in `docs/testing_artefacts/c3_mathematics_ix_ch04.md` and the tracker's C3 cell.

**§9: RELAXATION-ONLY, so this costs nothing** (founder ruling 2026-08-09, now written into
testing.md §9 as a standing carve-out). Every edit above only widens or permits; nothing is
tightened and no new obligation is created. Output authored under v1.2 therefore satisfies v1.3
by construction — and the clauses amended are the very ones ch 4 breached, so the installed
library becomes MORE compliant, not less. No re-author. The library was checked clause by clause
against v1.3 instead: `activity_title` 0 outside 6–13, `section_context` 0 outside 6–12, and
every method run of 3–4 sits on sections that genuinely converge (the standard's run of four
covers 4.7 → 4.8 → 4.8 → synthesis). Recorded in the tracker's C3 cell.
