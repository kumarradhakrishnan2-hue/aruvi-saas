#!/usr/bin/env python3
"""RETIRED same-day (2026-08-17), superseded by genon/resynth.py — safe to delete.

This held the CODA design: a per-chapter serve-only unit replacing the top's synthesis
at X = K+1, with its own serve mode, storage convention and staleness gate. The founder's
correction, an hour later: a second serve asset breaks the one-flow-across-eleven-stages
property and creates a maintenance surface for no need — instead the TOP'S SYNTHESIS
UNIT ITSELF is re-authored against the whole library (intersection-bounded, spent-device
aware), the serve law stays byte-identical, and any residual lightness is confined to the
top's identity serve. See docs/science_middle_stage_serve.md §6 v1.3 and genon/resynth.py.

Nothing imports this module. It exists only because the Cowork sandbox cannot delete
files; remove it freely.
"""
raise SystemExit(__doc__)
