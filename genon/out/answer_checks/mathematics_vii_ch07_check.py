#!/usr/bin/env python3
"""C3 answer-check worksheet — mathematics · vii · chapter 7

GENERATED by genon/extract_determinate.py — then FILLED IN BY HAND, which is the point.

For each item below, read the QUESTION and write the check yourself:
  * `claimed` = what the file ships (already filled in for you, as a sympy expression).
  * `target`  = what the question actually asks for, re-derived FROM THE STEM.

Transcribe from the stem. Do NOT read `method_one_line` first — at S4 the wrong answer shipped
beside a method line that had already found the right one, so a checker who copies the method
confirms the file against itself and sees nothing.

Where an answer is not symbolically expressible (a sentence, a units-bearing quantity, a
multi-part explanation), set `target = None` and record the verdict in the `note` — a judged
item is still a checked item, but say that it was judged.

Run it when filled. Every item must print OK or WRONG. Any WRONG is an S1 defect.
"""
from sympy import symbols, expand, simplify, factor, sympify, Rational, sqrt, Eq, solve

x, y, z, m, n, p, q, r, a, b, c, t, u, v = symbols('x y z m n p q r a b c t u v')

CHECKS = []  # (label, claimed, target, note)


# ---------------------------------------------------------------------------------------
# item 1  ·  ch_07_canonical.json  ·  MCQ  ·  verified=True
# Q: A triangle has sides of lengths 5 cm, 6 cm, and 5 cm. Which of the following correctly classifies this triangle by its sides?
# SHIPPED: B — Isosceles, because exactly two sides (5 cm and 5 cm) are equal and the third side (6 cm) is different.
# ---------------------------------------------------------------------------------------
CHECKS.append((
    "top #1",
    None,   # claimed  — transcribe the shipped answer above as a sympy expression
    None,   # target   — re-derive from the QUESTION, not from method_one_line
    "",     # note     — required when either side is None (judged, not computed)
))

# ---------------------------------------------------------------------------------------
# item 2  ·  ch_07_canonical.json  ·  MCQ  ·  verified=True
# Q: In a right-angled triangle, the side opposite the right angle is called the hypotenuse. If ∠B = 90° in triangle ABC, which side is the hypotenuse?
# SHIPPED: C — AC is the hypotenuse, as it is the side opposite vertex B where the right angle lies.
# ---------------------------------------------------------------------------------------
CHECKS.append((
    "top #2",
    None,   # claimed  — transcribe the shipped answer above as a sympy expression
    None,   # target   — re-derive from the QUESTION, not from method_one_line
    "",     # note     — required when either side is None (judged, not computed)
))

# ---------------------------------------------------------------------------------------
# item 3  ·  ch_07_canonical.json  ·  SCR  ·  verified=True
# Q: Three lengths are given: 4 cm, 5 cm, and 11 cm. Explain, without constructing, whether these lengths can form a triangle. Use the triangle inequality in your explanation.
# SHIPPED: No triangle is possible. The triangle inequality requires each side to be less than the sum of the other two. Checking the longest side: 11 must be less than 4 + 5 = 9, but 11 > 9, so the inequality is violated and no triangle exists.
# ---------------------------------------------------------------------------------------
CHECKS.append((
    "top #3",
    None,   # claimed  — transcribe the shipped answer above as a sympy expression
    None,   # target   — re-derive from the QUESTION, not from method_one_line
    "",     # note     — required when either side is None (judged, not computed)
))

# ---------------------------------------------------------------------------------------
# item 4  ·  ch_07_canonical.json  ·  SCR  ·  verified=True
# Q: Two sides of a triangle are 5 cm and 8 cm, and the angle between them is 175°. Explain whether a triangle can be formed with these measurements. What happens geometrically when the included angle approaches 180°?
# SHIPPED: A triangle cannot be formed when the included angle is 180° because the three points become collinear — the two sides lie along the same straight line. At 175° the angle arm is nearly straight and the third side is very short; as the included angle reaches 180°, the two endpoints of the given sides coincide on a line and no enclosed figure exists. For any included angle strictly less than 180° a triangle can be formed, so 175° technically does produce a (very flat) triangle, but the limiting case at 180° does not.
# ---------------------------------------------------------------------------------------
CHECKS.append((
    "top #4",
    None,   # claimed  — transcribe the shipped answer above as a sympy expression
    None,   # target   — re-derive from the QUESTION, not from method_one_line
    "",     # note     — required when either side is None (judged, not computed)
))

# ---------------------------------------------------------------------------------------
# item 5  ·  ch_07_canonical.json  ·  ECR  ·  verified=True
# Q: In triangle PQR, a line XY is drawn through P parallel to QR. Explain how this construction proves that the sum of the angles of any triangle is 180°. Name each pair of equal angles you use and state the geometric reason for their equality.
# SHIPPED: Draw XY through P with XY ∥ QR. Angle XPQ equals angle Q because they are alternate interior angles formed by transversal PQ cutting the parallel lines XY and QR. Angle YPR equals angle R because they are alternate interior angles formed by transversal PR cutting the same pair of parallel lines. The three angles at P — angle XPQ, angle QPR, and angle YPR — together make a straight angle (180°) along line XY. Substituting: angle Q + angle QPR + angle R = 180°, which is the angle-sum property.
# ---------------------------------------------------------------------------------------
CHECKS.append((
    "top #5",
    None,   # claimed  — transcribe the shipped answer above as a sympy expression
    None,   # target   — re-derive from the QUESTION, not from method_one_line
    "",     # note     — required when either side is None (judged, not computed)
))

# ---------------------------------------------------------------------------------------
# item 6  ·  ch_07_canonical.json  ·  NUM  ·  verified=True
# Q: A compass is set to a radius of 6 cm. A student draws a base segment AB of length 6 cm, then swings one arc of radius 6 cm centred at A and another arc of radius 6 cm centred at B. The arcs meet at point C. Without measuring, state the lengths of AC and BC, and explain why each equals 6 cm.
# SHIPPED: AC = 6 cm and BC = 6 cm. C lies on the arc centred at A with radius 6 cm, so by definition the distance from A to C equals 6 cm. C also lies on the arc centred at B with radius 6 cm, so the distance from B to C equals 6 cm. Because AB = AC = BC = 6 cm, triangle ABC is equilateral.
# ---------------------------------------------------------------------------------------
CHECKS.append((
    "top #6",
    None,   # claimed  — transcribe the shipped answer above as a sympy expression
    None,   # target   — re-derive from the QUESTION, not from method_one_line
    "",     # note     — required when either side is None (judged, not computed)
))

# ---------------------------------------------------------------------------------------
# item 7  ·  ch_07_canonical.json  ·  NUM  ·  verified=True
# Q: For each set of three lengths below, state whether a triangle can be formed. Show the key inequality check for each case. (a) 7, 9, 15 (b) 6, 6, 6 (c) 3, 8, 12
# SHIPPED: (a) 7 + 9 = 16 > 15, so a triangle is possible. (b) 6 + 6 = 12 > 6, so a triangle is possible (equilateral). (c) 3 + 8 = 11 < 12, so no triangle is possible — the longest side exceeds the sum of the other two.
# ---------------------------------------------------------------------------------------
CHECKS.append((
    "top #7",
    None,   # claimed  — transcribe the shipped answer above as a sympy expression
    None,   # target   — re-derive from the QUESTION, not from method_one_line
    "",     # note     — required when either side is None (judged, not computed)
))

# ---------------------------------------------------------------------------------------
# item 8  ·  ch_07_canonical.json  ·  NUM  ·  verified=True
# Q: Two circles are drawn: circle 1 centred at point A with radius 5 cm, and circle 2 centred at point B with radius 3 cm, where AB = 9 cm. Do the two circles intersect, touch at exactly one point, or not meet at all? Use the triangle-inequality reasoning to justify your answer.
# SHIPPED: For the two circles to intersect at two points, the three lengths 5, 3, and 9 (the two radii and the distance AB) must satisfy the triangle inequality. Check: 5 + 3 = 8, and 8 < 9, so the sum of the two radii is less than the distance between the centres. The circles do not intersect at all — they are too far apart. Geometrically, the circle of radius 3 centred at B lies entirely inside the gap and the two circles have no common point.
# ---------------------------------------------------------------------------------------
CHECKS.append((
    "top #8",
    None,   # claimed  — transcribe the shipped answer above as a sympy expression
    None,   # target   — re-derive from the QUESTION, not from method_one_line
    "",     # note     — required when either side is None (judged, not computed)
))

# ---------------------------------------------------------------------------------------
# item 9  ·  ch_07_canonical.json  ·  NUM  ·  verified=True
# Q: In triangle DEF, ∠D = 48° and ∠E = 93°. Find ∠F. Then find the exterior angle at vertex F (the angle formed between side EF extended beyond F and side DF). Show your working.
# SHIPPED: ∠D + ∠E + ∠F = 180°, so 48° + 93° + ∠F = 180°, giving ∠F = 180° − 141° = 39°. The exterior angle at F is supplementary to ∠F: exterior angle = 180° − 39° = 141°. Alternatively, the exterior angle equals the sum of the two non-adjacent interior angles: 48° + 93° = 141°. Both methods give 141°.
# ---------------------------------------------------------------------------------------
CHECKS.append((
    "top #9",
    None,   # claimed  — transcribe the shipped answer above as a sympy expression
    None,   # target   — re-derive from the QUESTION, not from method_one_line
    "",     # note     — required when either side is None (judged, not computed)
))

# ---------------------------------------------------------------------------------------
# item 10  ·  ch_07_canonical.json  ·  NUM  ·  verified=True
# Q: In triangle XYZ, ∠X = 2 × ∠Z and ∠Y = 60°. Find the values of ∠X and ∠Z.
# SHIPPED: Using the angle-sum property: ∠X + ∠Y + ∠Z = 180°. Substituting ∠Y = 60° and ∠X = 2∠Z: 2∠Z + 60° + ∠Z = 180°, so 3∠Z = 120°, giving ∠Z = 40° and ∠X = 80°. Check: 80° + 60° + 40° = 180°. ✓
# ---------------------------------------------------------------------------------------
CHECKS.append((
    "top #10",
    None,   # claimed  — transcribe the shipped answer above as a sympy expression
    None,   # target   — re-derive from the QUESTION, not from method_one_line
    "",     # note     — required when either side is None (judged, not computed)
))

# ---------------------------------------------------------------------------------------
# item 11  ·  ch_07_canonical.json  ·  SCR  ·  verified=True
# Q: A student folds a paper triangle so that the fold crease passes from vertex A and lands on side BC, forming a right angle with BC at the point where it meets BC. What is the geometric name for the crease, and what property of the crease confirms it is perpendicular to BC?
# SHIPPED: The crease is the altitude from vertex A to side BC. When the paper is folded so the two halves of BC coincide exactly along the crease, the crease makes equal angles on both sides of BC; equal angles that together form a straight angle must each be 90°, confirming the crease is perpendicular to BC. This is the definition of an altitude: the perpendicular segment from a vertex to the line of the opposite side.
# ---------------------------------------------------------------------------------------
CHECKS.append((
    "top #11",
    None,   # claimed  — transcribe the shipped answer above as a sympy expression
    None,   # target   — re-derive from the QUESTION, not from method_one_line
    "",     # note     — required when either side is None (judged, not computed)
))

# ---------------------------------------------------------------------------------------
# item 12  ·  ch_07_canonical.json  ·  SCR  ·  verified=True
# Q: A spider sits at one corner of a rectangular box whose length is 12 cm, width is 5 cm, and height is 0 cm (that is, a flat rectangular sheet). It wants to walk along the surface to the diagonally opposite corner. Explain how unfolding the surface helps find the shortest path, and state the length of that shortest path.
# SHIPPED: When the flat rectangle (12 cm × 5 cm) is unfolded it is already flat, so the shortest path along the surface is the straight diagonal. The diagonal length is √(12² + 5²) = √(144 + 25) = √169 = 13 cm. The unfolding principle works because a straight line on a flat (unfolded) surface is the shortest path between two points, mirroring the triangle-inequality idea that a direct path is shorter than any roundabout route.
# ---------------------------------------------------------------------------------------
CHECKS.append((
    "top #12",
    None,   # claimed  — transcribe the shipped answer above as a sympy expression
    None,   # target   — re-derive from the QUESTION, not from method_one_line
    "",     # note     — required when either side is None (judged, not computed)
))

# ---------------------------------------------------------------------------------------
# item 1  ·  ch_07_canonical_p07.json  ·  MCQ  ·  verified=True
# Q: A triangle has all three sides equal. Which of the following correctly describes this triangle using both classification systems?
# SHIPPED: A — Equilateral and acute-angled. Each angle of an equilateral triangle equals 60° (since 180° ÷ 3 = 60°), which is acute, so the triangle is both equilateral (by sides) and acute-angled (by angles).
# ---------------------------------------------------------------------------------------
CHECKS.append((
    "_p07 #1",
    None,   # claimed  — transcribe the shipped answer above as a sympy expression
    None,   # target   — re-derive from the QUESTION, not from method_one_line
    "",     # note     — required when either side is None (judged, not computed)
))

# ---------------------------------------------------------------------------------------
# item 2  ·  ch_07_canonical_p07.json  ·  SCR  ·  verified=True
# Q: A student claims that any three lengths can be used to construct a triangle as long as all three are positive. Give a specific counter-example with three positive lengths and explain, using the arc-intersection idea, why the construction fails for your example.
# SHIPPED: Counter-example: 2 cm, 3 cm, 8 cm. The longest side is 8 cm, but 2 + 3 = 5 < 8. When constructing, the arc of radius 2 cm from one base endpoint and the arc of radius 3 cm from the other do not reach each other — they fall short of intersecting — so no point C can be found and the triangle cannot be built.
# ---------------------------------------------------------------------------------------
CHECKS.append((
    "_p07 #2",
    None,   # claimed  — transcribe the shipped answer above as a sympy expression
    None,   # target   — re-derive from the QUESTION, not from method_one_line
    "",     # note     — required when either side is None (judged, not computed)
))

# ---------------------------------------------------------------------------------------
# item 3  ·  ch_07_canonical_p07.json  ·  ECR  ·  verified=True
# Q: In constructing a triangle from two angles and the included side, a student finds that the two angle arms drawn from the base endpoints run parallel and never meet. What must be true about the two given angles for this to happen? Justify your answer using the angle-sum property of triangles.
# SHIPPED: The two given angles must sum to exactly 180°. When the two base angles sum to 180°, the two arms drawn from the base endpoints are parallel (since they make co-interior angles summing to 180° with the base, which is the condition for parallel lines). If the arms are parallel, they never meet, so no vertex C can be formed. For a valid triangle, the angle-sum property requires all three angles to total 180°, which means the two base angles must sum to less than 180° so that the third angle is a positive value greater than 0°.
# ---------------------------------------------------------------------------------------
CHECKS.append((
    "_p07 #3",
    None,   # claimed  — transcribe the shipped answer above as a sympy expression
    None,   # target   — re-derive from the QUESTION, not from method_one_line
    "",     # note     — required when either side is None (judged, not computed)
))

# ---------------------------------------------------------------------------------------
# item 4  ·  ch_07_canonical_p07.json  ·  NUM  ·  verified=True
# Q: Using compass and ruler, construct an equilateral triangle with side length 5 cm. Write down the three steps of your construction procedure and state the length of each side of the completed triangle.
# SHIPPED: Step 1: Draw base AB = 5 cm. Step 2: Set compass to 5 cm and draw an arc centred at A. Step 3: Without changing the compass width, draw a second arc centred at B; mark point C where the two arcs intersect. Join AC and BC. Each side (AB, AC, BC) equals 5 cm because C lies on both arcs, each of radius 5 cm.
# ---------------------------------------------------------------------------------------
CHECKS.append((
    "_p07 #4",
    None,   # claimed  — transcribe the shipped answer above as a sympy expression
    None,   # target   — re-derive from the QUESTION, not from method_one_line
    "",     # note     — required when either side is None (judged, not computed)
))

# ---------------------------------------------------------------------------------------
# item 5  ·  ch_07_canonical_p07.json  ·  NUM  ·  verified=True
# Q: Determine which of the following sets of side lengths can form a triangle. For each set, show the key inequality check. Sets: (a) 7 cm, 11 cm, 15 cm; (b) 4 cm, 9 cm, 14 cm; (c) 6 cm, 6 cm, 11 cm.
# SHIPPED: (a) Longest side = 15; check 7 + 11 = 18 > 15. Triangle exists. (b) Longest side = 14; check 4 + 9 = 13 < 14. Triangle does not exist. (c) Longest side = 11; check 6 + 6 = 12 > 11. Triangle exists.
# ---------------------------------------------------------------------------------------
CHECKS.append((
    "_p07 #5",
    None,   # claimed  — transcribe the shipped answer above as a sympy expression
    None,   # target   — re-derive from the QUESTION, not from method_one_line
    "",     # note     — required when either side is None (judged, not computed)
))

# ---------------------------------------------------------------------------------------
# item 6  ·  ch_07_canonical_p07.json  ·  NUM  ·  verified=True
# Q: In triangle PQR, ∠P = 48° and ∠Q = 67°. Find ∠R. Then find the exterior angle at R (the angle formed by extending side QR beyond R).
# SHIPPED: ∠R = 180° − 48° − 67° = 65°. The exterior angle at R = 180° − 65° = 115°. Alternatively, by the exterior-angle theorem, exterior angle at R = ∠P + ∠Q = 48° + 67° = 115°. Both routes give 115°.
# ---------------------------------------------------------------------------------------
CHECKS.append((
    "_p07 #6",
    None,   # claimed  — transcribe the shipped answer above as a sympy expression
    None,   # target   — re-derive from the QUESTION, not from method_one_line
    "",     # note     — required when either side is None (judged, not computed)
))

# ---------------------------------------------------------------------------------------
# item 7  ·  ch_07_canonical_p07.json  ·  SCR  ·  verified=True
# Q: A triangle has vertices at three points on your paper. Describe the set-square-and-ruler steps to construct the altitude from the top vertex to the base. Then state what is special about the altitude in a right-angled triangle.
# SHIPPED: Steps: (1) Align the ruler along the base BC. (2) Place the set square so that one leg lies along the ruler. (3) Slide the set square along the ruler until its vertical edge passes through vertex A. (4) Draw the line segment from A to the point where the vertical edge meets BC (or its extension) — this is the altitude. In a right-angled triangle, one of the two legs is itself an altitude because it is already perpendicular to the other leg, which serves as the base.
# ---------------------------------------------------------------------------------------
CHECKS.append((
    "_p07 #7",
    None,   # claimed  — transcribe the shipped answer above as a sympy expression
    None,   # target   — re-derive from the QUESTION, not from method_one_line
    "",     # note     — required when either side is None (judged, not computed)
))

# ---------------------------------------------------------------------------------------
# item 1  ·  ch_07_canonical_p10.json  ·  MCQ  ·  verified=True
# Q: A triangle has sides of lengths 5 cm, 5 cm, and 7 cm. Which of the following correctly classifies this triangle by both its sides and its angles?
# SHIPPED: The triangle is isosceles (two sides equal: 5 cm, 5 cm) and acute-angled (all three angles are less than 90°, since the longest side squared is 49, which is less than 25 + 25 = 50).
# ---------------------------------------------------------------------------------------
CHECKS.append((
    "_p10 #1",
    None,   # claimed  — transcribe the shipped answer above as a sympy expression
    None,   # target   — re-derive from the QUESTION, not from method_one_line
    "",     # note     — required when either side is None (judged, not computed)
))

# ---------------------------------------------------------------------------------------
# item 2  ·  ch_07_canonical_p10.json  ·  MCQ  ·  verified=True
# Q: Which of the following combinations of triangle type is impossible?
# SHIPPED: A right-angled equilateral triangle is impossible. In an equilateral triangle all three angles must be equal and sum to 180°, so each angle is 60°; none can be 90°.
# ---------------------------------------------------------------------------------------
CHECKS.append((
    "_p10 #2",
    None,   # claimed  — transcribe the shipped answer above as a sympy expression
    None,   # target   — re-derive from the QUESTION, not from method_one_line
    "",     # note     — required when either side is None (judged, not computed)
))

# ---------------------------------------------------------------------------------------
# item 3  ·  ch_07_canonical_p10.json  ·  SCR  ·  verified=True
# Q: A student claims: 'If the sum of the two shorter sides of three given lengths equals the longest side, a triangle can still be formed.' Is the student correct? Justify your answer using the triangle inequality.
# SHIPPED: The student is incorrect. The triangle inequality requires each side to be strictly less than the sum of the other two. If the longest side equals the sum of the other two, the two arcs will just touch at one point and no triangle is formed — the three lengths are collinear.
# ---------------------------------------------------------------------------------------
CHECKS.append((
    "_p10 #3",
    None,   # claimed  — transcribe the shipped answer above as a sympy expression
    None,   # target   — re-derive from the QUESTION, not from method_one_line
    "",     # note     — required when either side is None (judged, not computed)
))

# ---------------------------------------------------------------------------------------
# item 4  ·  ch_07_canonical_p10.json  ·  ECR  ·  verified=True
# Q: In triangle PQR, ∠P = 65° and ∠Q = 75°. Using the angle-sum property, find ∠R. Then explain, using the parallel-line argument, why the three angles of any triangle must sum to 180°.
# SHIPPED: ∠R = 180° − 65° − 75° = 40°. The parallel-line argument: draw a line XY through vertex A parallel to BC. The alternate interior angles give ∠XAB = ∠B and ∠YAC = ∠C. At A, ∠XAB + ∠BAC + ∠YAC = 180° (angles on a straight line), so ∠B + ∠A + ∠C = 180°, i.e., the three angles of any triangle sum to 180°.
# ---------------------------------------------------------------------------------------
CHECKS.append((
    "_p10 #4",
    None,   # claimed  — transcribe the shipped answer above as a sympy expression
    None,   # target   — re-derive from the QUESTION, not from method_one_line
    "",     # note     — required when either side is None (judged, not computed)
))

# ---------------------------------------------------------------------------------------
# item 5  ·  ch_07_canonical_p10.json  ·  SCR  ·  verified=True
# Q: Two angles of a triangle are given as 95° and 90°. Explain, using the angle-sum property, why no triangle can be formed with these two angles.
# SHIPPED: The two given angles sum to 95° + 90° = 185°, which already exceeds 180°. Since the three angles of any triangle must sum to exactly 180°, there is no positive value left for the third angle, so no triangle can exist.
# ---------------------------------------------------------------------------------------
CHECKS.append((
    "_p10 #5",
    None,   # claimed  — transcribe the shipped answer above as a sympy expression
    None,   # target   — re-derive from the QUESTION, not from method_one_line
    "",     # note     — required when either side is None (judged, not computed)
))

# ---------------------------------------------------------------------------------------
# item 6  ·  ch_07_canonical_p10.json  ·  NUM  ·  verified=True
# Q: Using only a compass and ruler, describe the steps to construct a triangle with all three sides equal to 6 cm. Then verify, using the arc-radius argument, that both non-base sides are indeed 6 cm.
# SHIPPED: Steps: (1) Draw base AB = 6 cm. (2) Open compass to 6 cm and draw an arc centred at A. (3) Without changing the compass, draw an arc centred at B. (4) Mark point C at the intersection of the two arcs. (5) Draw AC and BC. Verification: C lies on the arc of radius 6 cm centred at A, so AC = 6 cm. C also lies on the arc of radius 6 cm centred at B, so BC = 6 cm. Both non-base sides equal the base, confirming the triangle is equilateral.
# ---------------------------------------------------------------------------------------
CHECKS.append((
    "_p10 #6",
    None,   # claimed  — transcribe the shipped answer above as a sympy expression
    None,   # target   — re-derive from the QUESTION, not from method_one_line
    "",     # note     — required when either side is None (judged, not computed)
))

# ---------------------------------------------------------------------------------------
# item 7  ·  ch_07_canonical_p10.json  ·  NUM  ·  verified=True
# Q: Three lengths are given: 8 cm, 5 cm, and 14 cm. Without constructing, determine whether a triangle is possible. Show your working using the triangle inequality. If a triangle is possible, state which arc-intersection case (intersecting, touching, or non-intersecting) would appear.
# SHIPPED: Check the critical comparison: the longest side is 14 cm. Sum of the other two: 8 + 5 = 13 cm. Since 14 > 13, the longest side exceeds the sum of the other two. The triangle inequality is violated. No triangle is possible. If constructed, the arcs centred at the two base endpoints would not intersect — the circles would be non-intersecting (the longer arc falls short of reaching the shorter one).
# ---------------------------------------------------------------------------------------
CHECKS.append((
    "_p10 #7",
    None,   # claimed  — transcribe the shipped answer above as a sympy expression
    None,   # target   — re-derive from the QUESTION, not from method_one_line
    "",     # note     — required when either side is None (judged, not computed)
))

# ---------------------------------------------------------------------------------------
# item 8  ·  ch_07_canonical_p10.json  ·  NUM  ·  verified=True
# Q: Describe the steps to construct a triangle with two sides of 5 cm and 8 cm and an included angle of 50°. Then find the third angle if the other two angles of the triangle are 50° and 80°.
# SHIPPED: Construction steps (SAS): (1) Draw base AB = 8 cm. (2) At A, use a protractor to mark an angle of 50°. (3) Along the angle arm from A, mark point C at 5 cm. (4) Draw BC to complete the triangle. Third angle: ∠A + ∠B + ∠C = 180°; if ∠A = 50° and ∠B = 80°, then ∠C = 180° − 50° − 80° = 50°.
# ---------------------------------------------------------------------------------------
CHECKS.append((
    "_p10 #8",
    None,   # claimed  — transcribe the shipped answer above as a sympy expression
    None,   # target   — re-derive from the QUESTION, not from method_one_line
    "",     # note     — required when either side is None (judged, not computed)
))

# ---------------------------------------------------------------------------------------
# item 9  ·  ch_07_canonical_p10.json  ·  SCR  ·  verified=True
# Q: A triangle has a base along a horizontal line and the opposite vertex above the base. Describe how you would use a set square and ruler to construct the altitude from the top vertex to the base. Then explain why the paper-folding method also produces a perpendicular to the base.
# SHIPPED: Set-square method: align the ruler along the base; place the set square with one of its right-angle edges against the ruler; slide the set square along the ruler until its vertical edge passes through the top vertex; draw the segment from the vertex down to the base along the vertical edge — this is the altitude. Paper-folding: when the top vertex is folded onto the base, the crease lies equidistant from every pair of corresponding points on the two layers; this equal-distance property means the crease is perpendicular to the base at the foot.
# ---------------------------------------------------------------------------------------
CHECKS.append((
    "_p10 #9",
    None,   # claimed  — transcribe the shipped answer above as a sympy expression
    None,   # target   — re-derive from the QUESTION, not from method_one_line
    "",     # note     — required when either side is None (judged, not computed)
))

# ---------------------------------------------------------------------------------------
# item 10  ·  ch_07_canonical_p10.json  ·  NUM  ·  verified=True
# Q: Three side-length sets are listed below. For each, state without constructing whether a triangle exists, and justify your answer. Then, for the one set that does form a triangle, state what type it is by sides. | Set | Side lengths | |-----|--------------| | (a) | 6, 6, 11 | | (b) | 4, 7, 12 | | (c) | 9, 9, 9 |
# SHIPPED: (a) 6 + 6 = 12 > 11: triangle exists. Type: isosceles. (b) 4 + 7 = 11 < 12: longest side 12 exceeds sum of other two; no triangle. (c) 9 + 9 = 18 > 9: triangle exists. Type: equilateral. So sets (a) and (c) form triangles; set (b) does not.
# ---------------------------------------------------------------------------------------
CHECKS.append((
    "_p10 #10",
    None,   # claimed  — transcribe the shipped answer above as a sympy expression
    None,   # target   — re-derive from the QUESTION, not from method_one_line
    "",     # note     — required when either side is None (judged, not computed)
))


# ---------------------------------------------------------------------------------------
if __name__ == "__main__":
    wrong = judged = 0
    for label, claimed, target, note in CHECKS:
        if claimed is None or target is None:
            judged += 1
            verdict = "JUDGED"
            if not note:
                verdict = "INCOMPLETE — a None side needs a note"
                wrong += 1
        else:
            ok = simplify(sympify(claimed) - sympify(target)) == 0
            verdict = "OK" if ok else "WRONG"
            wrong += 0 if ok else 1
        print(f"{verdict:12} {label}" + (f"  | {note}" if note else ""))
    print(f"\n{len(CHECKS)} determinate items · {judged} judged · {wrong} WRONG")
    raise SystemExit(1 if wrong else 0)
