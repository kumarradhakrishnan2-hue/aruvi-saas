--- genon/out/stage_prep_mathematics_preparatory/assess_middle_v3.4_pre_tickline.txt	2026-08-10 20:24:50.192107531 +0530
+++ data/content/constitutions/assessment/mathematics/middle/assessment_constitution.txt	2026-08-11 13:59:24.024925229 +0530
@@ -1,4 +1,4 @@
-ARUVI — MATHEMATICS ASSESSMENT CONSTITUTION · VERSION 3.4
+ARUVI — MATHEMATICS ASSESSMENT CONSTITUTION · VERSION 3.5
 Stages: Foundational, Preparatory, Middle, Secondary
 
 ================================================================================
@@ -201,10 +201,17 @@
   is the header. Use only for tabular data the student must read or
   fill in.
 
-- Number line: one line tagged `number_line:` then the ticks split by "|",
-  each cell a number (labelled tick) or "..." (blank tick the student marks);
-  endpoints must be numbers. E.g. `number_line: -3 | ... | 0 | ... | 3`.
-  Keep the task wording in `prompt`. Do not emit a number line as a pipe-table.
+- Tick line (tagged `number_line:`): one line tagged `number_line:` then the
+  ticks split by "|". Each cell is either a LABEL — a number, or a short word
+  naming what sits at that tick — or "..." (a blank tick the student fills in).
+  At least three cells; a label is a label, not a sentence. The ticks are drawn
+  as an ordered line, never as a grid.
+    numbers:  `number_line: -3 | ... | 0 | ... | 3`
+    words:    `number_line: shorter | ... | ... | longer`
+  Keep the task wording in `prompt`. Do not emit a tick line as a pipe-table,
+  and do not tag a pipe-table as a tick line: a stimulus that declares this tag
+  and does not satisfy it is rejected at certification and renders as plain
+  prose, losing the picture it asked for.
 
 - Empty string "": no visual stimulus. This is the default for almost
   all geometry items — the figure, if needed, is reached via the
@@ -389,5 +396,5 @@
 
 ================================================================================
 Aruvi · NCF 2023-Aligned Pedagogical Platform · Knowledge Commons
-Mathematics Assessment Constitution · Version 3.4 · Internal Document
+Mathematics Assessment Constitution · Version 3.5 · Internal Document
 ================================================================================
