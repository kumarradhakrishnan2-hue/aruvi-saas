# C3 — rule-by-rule table · S7 mathematics · middle · pilot chapter VII ch 7

**Artefacts checked**
- `data/content/saved_plans/mathematics/vii/ch_07_canonical.json` — the STANDARD canonical, 12 units × 40 min
- `data/content/saved_plans/mathematics/vii/ch_07_canonical_p07.json` — the FLOOR compact, 7 units × 40 min

**Constitutions (live files, read 2026-08-10 after today's amendments)**
- LP `data/content/constitutions/lesson_plan/mathematics/middle/lesson_plan_constitution.txt` — **v3.6**
- Assessment `data/content/constitutions/assessment/mathematics/middle/assessment_constitution.txt` — **v3.4**

**Source of truth for items/goals** `data/content/chapters/mathematics/vii/summaries/ch_07_summary.json`
(5 sections `section 7.1`–`section 7.5`; `enumerated_activities` = **1** (`A-1`);
`enumerated_worked_examples` = **[] — ZERO**; `enumerated_exercises` = **30** (`E-1`…`E-30`)).

**Provenance note.** Both files stamp `genon_canonical.constitution = "LP v3.6 / assessment v3.3"` —
i.e. they were AUTHORED under assessment **v3.3** and brought to v3.4 (distractors-only) by
`repair_option_reveals.py`. This table judges them against the **live v3.4** text, as C3 requires.

**Verdict tally**

| File | PASS | SUBJECTIVE-PASS | FAIL | N/A |
|---|---|---|---|---|
| `ch_07_canonical` | 14 | 3 | 8 | 1 |
| `ch_07_canonical_p07` | 15 | 4 | 6 | 1 |

(26 rows per file = LP Rules 1–11 + VOCABULARY + THE SELF-CONTAINED REGISTER + LP JSON SCHEMA,
and assessment Rules 1–10 + ANCHORING + ASSESSMENT JSON SCHEMA.)

---

## Table 1 — LESSON PLAN CONSTITUTION v3.6

| Rule # | Rule (one clause) | ch_07_canonical | ch_07_canonical_p07 | Evidence |
|---|---|---|---|---|
| **LP 1** | LP walks textbook section order; each unit anchors **one or at most two adjacent sections**; a section's units are contiguous and never revisited | **FAIL** | **PASS** | *Contiguity (the repaired half) holds on both.* Canonical runs after `repair_unit_order.py` are `7.1` (u1) · `7.2` (u2,3,4) · `7.3` (u5,6,7,8) · `7.4` (u9) · `7.5` (u10,11) · synthesis (u12) — **zero revisits**, confirming the declared repair (`genon_canonical.repairs[0]`: *"unit 11 anchored section 7.2 after the plan had moved through 7.3, 7.4 and 7.5. Moved to sit beside units 2 and 3"*, `order: [1,2,3,11,4,5,6,7,8,9,10,12]`). p07 as authored: `7.1`(1) · `7.2`(2,3) · `7.3`(4,5) · `7.4`(6) · `7.5`(7), one segment per unit, zero revisits. **The canonical FAILS the cardinality clause:** unit 12 carries **five** `textbook_segments` — `"section 7.1"`, `"section 7.2"`, `"section 7.3"`, `"section 7.4"`, `"section 7.5"` (`activity_title: "Whole-Chapter Synthesis: Constructing, Classifying, and Proving"`, `synthesis: true`) — against "one or at most two adjacent sections", and it re-anchors four sections the plan had left, against "a later period MUST NOT re-anchor a section an earlier run already completed". **Cause is the v2.0 synthesis mandate in the variant brief, which the constitution has no carve-out for** — a rule/brief collision, not a generation slip (see the founder note at the end). |
| **LP 2** | Allocate exactly B units in section order; every summary section appears; no numerical formula; no front-loading | **PASS** | **PASS** | Canonical: 12 units = `period_schedule {duration_minutes: 40, count: 12}`; all five sections appear. Spread 1·3·4·1·2 tracks substance (section 7.2 carries 9 enumerated exercises, 7.3 carries 11, 7.1 three, 7.4 two, 7.5 five) — no arithmetic pattern, no tail compression. p07: 7 units, spread 1·2·2·1·1, all five sections present. Two-element `section_goal` splits obey "earlier units first-listed goal, later units the second": canonical 7.2 = reason(u2) → apply(u3,u4); 7.3 = reason(u5,u6) → apply(u7,u8); p07 7.2 = reason(u2) → apply(u3); 7.3 = reason(u4) → apply(u5). Single-goal sections never split: 7.1 apply, 7.4 apply, 7.5 recall throughout (canonical u10 AND u11 both `"section_goal": "recall"`). |
| **LP 3** | Every `id` in `textbook_items_in_class` / `homework` equals exactly a summary `id`; no invented IDs, no `-a`/`-b` suffixes | **PASS** | **PASS** | Mechanical set check against the summary: canonical uses `E-1…E-30`, `A-1` only; p07 likewise. **The `WE-n` fabrication risk did NOT materialise — `enumerated_worked_examples` is `[]` and neither file contains a single `WE-` token** (`grep -o 'WE-[0-9]'` → 0 hits in both). No suffixed IDs. Legitimate multi-unit reuse is present and constitutional: canonical u12 relists `E-23` (also u7) and `E-29` (also u10) — "each period lists the same single ID". |
| **LP 4** | Each unit's `section_goal` equals a goal the summary declares for that unit's anchored section(s) | **PASS** | **PASS** | Summary declares `7.1 [apply]`, `7.2 [reason, apply]`, `7.3 [reason, apply]`, `7.4 [apply]`, `7.5 [recall]`. Every unit checked: canonical u1 apply/7.1 ✓, u2 reason/7.2 ✓, u3–u4 apply/7.2 ✓, u5–u6 reason/7.3 ✓, u7–u8 apply/7.3 ✓, u9 apply/7.4 ✓, u10–u11 recall/7.5 ✓, u12 apply — satisfied by the last clause ("a goal declared by **at least one** of those sections"; 7.1/7.2/7.3/7.4 all declare `apply`). p07: u1 apply/7.1 ✓, u2 reason ✓, u3 apply ✓, u4 reason/7.3 ✓, u5 apply/7.3 ✓, u6 apply/7.4 ✓, u7 recall/7.5 ✓. No goal is invented. |
| **LP 5** | One method per unit from the five; same method not in >2 consecutive units; each method once per chapter; default goal→method mapping binds | **SUBJECTIVE-PASS** | **SUBJECTIVE-PASS** | Enum and diversity are objective PASSes: canonical `Inductive · Discovery · Problem-solving · Discovery · Inductive · Deductive · Problem-solving · Discovery · Play-way · Inductive · Problem-solving · Problem-solving` — longest run **2** (u11–u12), all five methods present. p07 `Inductive · Discovery · Problem-solving · Deductive · Problem-solving · Play-way · Discovery` — longest run 1, all five present. **Neither file relies on v3.5's content-driven exception** ("The cap YIELDS where the anchored sections genuinely converge"), so that amendment is untested by this pair. The subjective part is the default mapping (`recall → Discovery, Inductive · reason → Deductive, Inductive · apply → Problem-solving, Play-way`), which the same rule says "still binds": canonical departs in **5 of 12** units — u1 `apply`→`Inductive`, u2 `reason`→`Discovery`, u4 `apply`→`Discovery`, u8 `apply`→`Discovery`, u11 `recall`→`Problem-solving`; p07 in **2 of 7** — u1 `apply`→`Inductive`, u2 `reason`→`Discovery`. Judged PASS because each departure is content-defensible on its own band text (u2's *"Students attempt Math Talk … The expectation is that the arcs do not intersect; students observe this directly"* is discovery in substance, and u11's *"Try This Q3 … How many different triangles exist"* is a problem to solve, not a fact to recall) — but this is a judgement, and a founder who reads "binds" strictly would score both files FAIL here. |
| **LP 6** | Bands sum exactly to `period_duration_minutes`; minimum 3 bands; ranges tile 0→duration with no gaps/overlaps; no overrun | **PASS** | **PASS** | Every band parsed and tiled programmatically. Canonical: 11 units × 4 bands + u11 × 3 bands, every unit closing exactly at 40 (`"0–8","8–22","22–32","32–40"` … `"0–16","16–34","34–40"`). p07: 6 units × 4 bands + u6 × 3 (`"0–12","12–22","22–40"`), all closing at 40. Zero gaps, zero overlaps, zero units under 3 bands, `period_duration_minutes` = 40 on all 19 units (INPUTS 4's single class-standard row: `period_rows_snapshot [{duration: 40, count: 12}]` / `[{duration: 40, count: 7}]`). |
| **LP 7** | Chapter header table appears once at the top: `chapter_number`, `chapter_title`, `core_cg`, `core_competencies`, `adjunct_competencies`, `periods_allocated` | **FAIL** | **FAIL** | `chapter_number: 7` and `chapter_title: "A Tale of Three Intersecting Lines"` are present; **`core_cg`, `core_competencies`, `adjunct_competencies` and `periods_allocated` are absent from both files** (`grep -c core_cg` → 0; `grep -c periods_allocated` → 0). This is not a save-format artefact: the **model's own raw output** `genon/out/canonical/mathematics/vii/ch_07_20260810_184152_raw.txt` also carries none of them (top-level keys are only `grade, subject, chapter_number, chapter_title, period_schedule, lesson_plan, coverage_handoff, assessment_items`). **Platform-wide, not chapter-specific** — the certified S1 file `social_sciences/ix/ch_03_canonical.json` and the S4 file `mathematics/ix/ch_04_canonical.json` are identical in this respect. Recommended as a pipeline/constitution reconciliation, not a re-author. |
| **LP 8** | LP body text must not reference C-codes; competencies appear only in the header/mapping JSON | **PASS** | **PASS** | Regex `\bC-\d|competenc` over every `activity_title`, `time_bands[].activity`, `teacher_notes` and `materials` in both files → **0 hits**. (Vacuously helped by LP 7's missing header, which is scored separately above.) |
| **LP 9** | `homework` is `[]` by default; 1–2 items only when justified; not consumed downstream | **PASS** | **PASS** | Canonical: units 5, 8, 9, 11, 12 carry `"homework": []`; the seven populated units carry exactly **one** item each (`E-3, E-6, E-10, E-7, E-17, E-21, E-27`). p07: units 2, 4, 6 empty; units 1, 3, 5, 7 carry one each (`E-3, E-6, E-22, E-30`). Never 3+. No assessment item consumes a homework id. |
| **LP 10** | Band narration names the item by `book_ref` + its `description` in brackets and inverted commas, **≤10 words** with `....` if truncated; no internal IDs; bound by THE REGISTER | **FAIL** | **FAIL** | Format and the ID ban both hold — every invoked item is named by `book_ref` (`Figure it Out Q1, section 7.2 p.156 ("…")`), and regex `\b(A\|E\|WE)-\d+\b` over all band text and teacher_notes → **0 hits** in both files. **The 10-word cap is breached almost universally.** Canonical: **26 of 26** bracketed descriptions exceed 10 words. Worst: u4 `6–26` at **33 words** — `("Find 3 examples each of sidelength sets where the two circles: (a) touch each other at a point; (b) do not intersect. Frame a complete procedure to check the existence of a triangle....")`; u11 `16–34` at 31; u10 `22–36` and u12 `24–34` at 29. Even the shortest, u1 `8–22` `("Construct triangles with these sidelengths (all in cm) using compass and ruler....")`, is 12. p07 is tighter but still **15 of 20** over: u6 `0–12` at 23, u7 `10–22` at 23, u7 `22–34` at 23; the five compliant ones are 11–12 words at u5 `32–40` and u4. Secondary defect: the bracketed text is meant to be the summary `description` truncated, but two are **edited mid-string, not truncated** — canonical u6 `22–34` `("Find the third angle using a parallel-line argument when two angles are: (a) 36°, 72°....")` against the summary's `"Find the third angle (using a parallel-line argument) when two angles are: …"`, and p07 u7 `22–34` `("Explore whether it is possible to construct: a right-angled equilateral triangle; …")` against the summary's `"Through construction, explore whether it is possible to construct: (a) a right-angled …"`. |
| **LP 11** | `coverage_handoff`: one entry per unit, routed by goal, in unit order within each cluster; `section_ref` = the section declaring the unit's goal; `anchor_id` drawn from **that unit's own** `textbook_items_in_class[]` then `homework[]`, priority exercise → worked_example → activity | **FAIL** | **FAIL** | Counts and ordering are right on both: canonical 2 + 3 + 7 = **12 entries for 12 units**; p07 1 + 2 + 4 = **7 for 7**. Clusters are correctly keyed (`section_a ["recall"]`, `section_b ["reason"]`, `section_c ["apply"]`) and every cluster reads in unit order. Anchors are verbatim from the summary (`anchor_book_ref`/`anchor_description` byte-compared — 0 mismatches) and unique across the handoff. **Two anchor-selection breaches, one per file.** (a) **Canonical, `section_c` entry 7 (the unit-12 entry): `"section_ref": "section 7.5", "anchor_id": "E-30"`.** Unit 12's own pools are `textbook_items_in_class = [E-23, E-29]` and `homework = []`; **`E-30` appears nowhere in unit 12**, so step 1/2 of Anchor selection is bypassed — and `section 7.5` is the one anchored section whose summary entry does **not** declare `apply` (`"section_goal": ["recall"]`), against "the primary section is the one whose summary declares the period's `section_goal`". Expected under the rule: `section_ref` one of 7.1–7.4 with `anchor_id: "E-23"`. This one entry is what produces the weakest assessment item in the pair (see A3, `Q-C-7`). (b) **p07, `section_c` entry 4: `"anchor_id": "E-25"`** for unit 6, whose array is `[A-1 (activity), E-24 (exercise), E-25 (exercise)]` — `E-24` is an unused exercise standing **earlier in array order**, so "walk array order and pick the first item whose id has NOT yet been used … applying type priority exercise → worked_example → activity" selects `E-24`, not `E-25`. Consequence is visible downstream: p07's `Q-C-4.exercise.book_ref = "Figure it Out, section 7.4 p.169"` where the rule yields p.168. |
| **VOCABULARY** | "unit" in all teacher-facing prose; "period"/"session" only in schema field names and the scheduling budget | **PASS** | **PASS** | Regex `\b(period\|periods\|session\|sessions)\b` over every `activity_title`, band `activity`, `teacher_notes` and every assessment `prompt`/`expected_answer`/`method_one_line`/`inclusivity` string → **0 hits in both files**. "unit" is used correctly where used: canonical u4 *"This unit deepens the triangle-inequality work…"*, u12 *"This synthesis unit deliberately does not assume any prior activity…"*. |
| **THE SELF-CONTAINED REGISTER** | No clock quantity · no forward reference or completion claim · no calendar time | **SUBJECTIVE-PASS** | **SUBJECTIVE-PASS** | `register_scan.py` reports 0 hits; **read independently here as testing.md C5 instructs**, and the read agrees, with two strings a founder should see. Ban 1 holds and holds *in kind*, which is what the rule asks: *"Quick mental warm-up"* (canonical u3 `0–5`), *"A short group challenge"* (u7 `28–38`), *"Brief consolidation"* (u5 `36–40`), *"Try This Q3 … is rich and time-intensive"* (u3 notes) — no minute counts, no "the remaining time", no "half the session". Ban 3 holds: no `today/tomorrow/this week/next class/day-name` token in either file. Ban 2 is where the judgement sits. Backward continuity is exemplary and content-named, exactly as the register prescribes — canonical u6 notes *"Having proved that alternate angles on a parallel line are equal, the step from construction failure to a formal proof of the angle sum is a short deductive move"*; u3 notes *"Having established through experimentation that arcs fail to intersect when the triangle inequality is violated, students are now formalising that reasoning into a procedure"*; p07 u2 `0–6` *"The teacher reminds students that the two-arc method (established in equilateral triangle construction) depends on the arcs actually intersecting"*. **Two canonical strings lean toward a completion claim and one points forward out of the chapter:** u11 `34–40` *"connecting the geometric intuition built throughout the chapter"*; u12 `0–10` *"warming up all five sections' ideas without recapping any single unit"* — structurally the shape of the rule's own counter-example *"having covered all three angle pairs"*, although here it is backward-looking and sits in the mandated synthesis unit, which by construction is served only after the rest; and u10 `36–40` *"Teacher notes that the full relationship between the two systems is explored further in a later chapter"* — a forward pointer, but to the TEXTBOOK's later chapter (mirroring the summary's own *"defers full exploration … to a later chapter"*), not to a unit of this plan, so outside the ban's stated rationale. p07 carries none of these. Recorded as a judgement, not a clean read. |
| **LP JSON SCHEMA** | Field presence, types, `activity_title` ≤12 words, `teacher_notes` 2–3 sentences, `textbook_segments` 1–2 entries, `time_bands.minutes` string range | **FAIL** | **FAIL** | Per-unit fields are complete and correctly typed on every unit of both files (`period_number, period_duration_minutes, textbook_segments{ref,title}, section_goal, activity_title, pedagogical_method, textbook_items_in_class{id,type,source_section,book_ref,description}, homework, time_bands{minutes,activity}, teacher_notes, materials`); `type` values are only `activity`/`exercise`; `section_goal` only `recall`/`reason`/`apply`; `textbook_segments[].title` matches the summary verbatim on all 19 units; `activity_title` word counts are 6–11 (cap 12) on both; `teacher_notes` is 2–3 sentences everywhere and no note opens with "Transition" or a section label; band `minutes` are en-dash string ranges as specified. **FAIL on two counts, both shared:** (i) the top-level block is missing `core_cg`, `core_competencies`, `adjunct_competencies`, `periods_allocated` **and `dissolution_test`** (see LP 7 — absent in the raw model output too); (ii) canonical unit 12 carries **5** `textbook_segments` against the schema's `// 1–2 entries` (see LP 1). p07 is clean on (ii). |

---

## Table 2 — ASSESSMENT CONSTITUTION v3.4

| Rule # | Rule (one clause) | ch_07_canonical | ch_07_canonical_p07 | Evidence |
|---|---|---|---|---|
| **A 1** | Three sections A/B/C fed by `handoff.section_a/b/c`; items inside a section appear in handoff order; empty cluster → empty section | **PASS** | **PASS** | Both files emit exactly `[{section_code:"A", section_title:"Recall and Apply"}, {"B","Reason and Explain"}, {"C","Apply in Context"}]` with `"note": ""`. Handoff order is preserved item-for-item, verified through each item's `exercise.book_ref`: canonical C = `Construct §7.1 p.150 (E-1) → FIO Q1 §7.2 p.156 (E-8) → FIO §7.2 p.158 (E-9) → FIO Q2 §7.3 p.163 (E-18) → Math Talk §7.3 p.164 (E-19) → FIO §7.4 p.168 (E-24) → FIO §7.5 p.172 (E-30)`, matching `section_c.goals[0..6]` exactly; A and B likewise; p07 likewise. Cosmetic only, no rule: the canonical's `id`s run `Q-C-1, Q-C-2, Q-C-6, Q-C-3, Q-C-4, Q-C-5, Q-C-7` — the *order* is right, the numbering is not sequential. |
| **A 2** | Exactly one item per `cluster.goals` entry; no bonus / mixed-review / chapter-wrap items | **PASS** | **PASS** | Canonical: A=2, B=3, C=7 → **12 items against 12 handoff goals** (2/3/7). p07: A=1, B=2, C=4 → **7 against 7** (1/2/4). No extra item, no wrap-up item — notably the canonical's synthesis unit produced **no** additional item, which is the correct reading of the rule. |
| **A 3** | Prompt freshly generated and solvable from the section's (and prior sections') `prose_summary`; **must not copy the anchor's numbers, entities or wording**; out-of-chapter procedures prohibited; cognitive level matches `goal` | **FAIL** | **SUBJECTIVE-PASS** | **Canonical — two breaches, both in the copy clause, one compounded by an out-of-chapter method.** (i) `Q-A-1` prompt: *"A triangle has sides of lengths **5 cm, 6 cm, and 5 cm**. Which of the following correctly classifies this triangle by its sides?"* against its own anchor `E-26` = *"Construct triangle ABC with **BC = 5 cm, AB = 6 cm, CA = 5 cm**"* — the anchor's specific numbers copied outright. (ii) `Q-C-7` prompt: *"**A spider** sits at one corner of a rectangular **box** … It wants to walk along the surface to the **diagonally opposite corner**"* against anchor `E-30` = *"Shortest Path in a Box: **a spider** at one corner of a **box** wants to walk (on the surfaces) to the **opposite corner**"* — named entity and wording copied. `Q-C-7` additionally leaves the chapter: its answer is *"√(12² + 5²) = √(144 + 25) = √169 = 13 cm"* and its `method_one_line` names *"the Pythagorean relationship 12² + 5² = 13²"*, while `grep -c Pythagor` on `ch_07_summary.json` = **0** — a prohibited out-of-chapter procedure. The same notation leaks into `Q-A-1` option C (*"Right-angled, because 5² + 5² equals 6²"*) and its diagnostic (*"5² + 5² = 50 ≠ 36 = 6²"*). `Q-C-7` is also the item whose `goal: "apply"` sits on `section_ref: "section 7.5"`, a recall-only section — the downstream face of the LP 11 breach. **p07 — no copying of numbers or entities anywhere**; the one judgement call is `Q-C-4`'s prompt *"Describe the set-square-and-ruler steps to construct the altitude from the top vertex to the base"* against anchor `E-25` = *"Construct the altitude from A to BC using ruler and set square"* — the same procedure named in nearly the same words, though generalised off the anchor's labelled vertices and extended with a second question. Judged PASS, but it is close to the line. Cognitive levels match `goal` on all 19 items in both files (recall → classify/name; reason → *"Explain … Justify your answer using the angle-sum property"*; apply → fresh numerical work). |
| **A 4** | Answer verified by two independent routes before emission; model's verification reasoning must not appear in any emitted field | **SUBJECTIVE-PASS** | **SUBJECTIVE-PASS** | `verified: true` on **all 12 and all 7** items; regex for self-correction leakage (`wait,`, `let me`, `re-check`, `actually,`) → **0 hits in both files**, so the ARV-D-084/ARV-D-085 leak shape is absent. Every determinate answer I re-derived from the stem agrees with the file (canonical: `Q-B-1` 4+5=9<11 → impossible ✓; `Q-C-2` (a) 16>15 ✓ (b) equilateral ✓ (c) 11<12 ✓; `Q-C-3` ∠F = 39°, exterior 141° ✓; `Q-C-4` ∠Z = 40°, ∠X = 80° ✓; `Q-C-6` 5+3 = 8 < 9 → circles do not meet ✓; `Q-C-7` 13 cm arithmetically ✓ though by a banned method; p07: `Q-A-1` 60° acute ✓; `Q-B-1` 2+3 = 5 < 8 ✓; `Q-B-2` sum 180° ✓; `Q-C-2` 18>15 ✓ / 13<14 ✓ / 12>11 ✓; `Q-C-3` ∠R = 65°, exterior 115° ✓). **Marked SUBJECTIVE because two things are judgement, not proof:** (a) the formal C3 maths sub-check (`genon/extract_determinate.py` worksheet, target transcribed from the stem before `method_one_line` is read) is a separate artefact and is **still owed** — my re-derivation above was done with the guide visible and therefore does not discharge it; (b) canonical `Q-B-2` reads as unresolved deliberation surfacing in `expected_answer`: the stem asks whether a triangle exists with an included angle of **175°**, and the answer opens *"A triangle cannot be formed when the included angle is 180°…"* before conceding *"so 175° technically does produce a (very flat) triangle, but the limiting case at 180° does not"*. It reaches the right place but does not answer the question asked first. Founder-visible. |
| **A 5** | Verification failure → blanked `prompt`/`options`/`teacher_guide` with `verified: false`; empty `anchor_id` does NOT trigger fallback | **N/A** | **N/A** | **No fallback item exists in either file** — `verified: true` on every item, no empty `prompt`, no `options: []` on an MCQ, no `inclusivity.support` of the form `"Refer to the book exercise(s) anchored to …"`. Nothing to judge; the rule is untested by this pair. (The second clause is also untested: every handoff entry carried a non-empty `anchor_id`, so no item had to stand without an exercise companion.) |
| **A 6** | `teacher_guide` object: prose `expected_answer`; one-sentence `method_one_line`; `what_each_option_reveals` = **one entry per NON-CORRECT option, correct omitted**; `inclusivity` two bare clauses **together ≤ 25 words** | **FAIL** | **FAIL** | **The distractors-only repair holds on both** — canonical `Q-A-1` correct `B`, keys `{A, C, D}`; `Q-A-2` correct `B`, keys `{A, C, D}`; p07 `Q-A-1` correct `A`, keys `{B, C, D}`: **key set == non-correct labels exactly in all three MCQs**, so ARV-D-092's mechanical check now passes. **Three FAILs against it.** (i) **The 25-word `inclusivity` cap is breached on every single item of both files**, by 1.6× to 2.8×: canonical 49/41/54/56/56/60/59/**69**/54/42/51/**68** words (`Q-C-6` at 69 — *"Draw two points A and B 9 cm apart on paper, draw rough circles of radius 5 and 3 around them, and look at whether the circles reach each other."* + a 39-word challenge); p07 53/61/**71**/47/66/60/53. They are also full sentences, not "one short clause". (No item prefixes "Support:"/"Challenge:", which the rule separately forbids — that half holds.) (ii) **Canonical `Q-A-2.expected_answer` names the wrong option letter:** *"**C** — AC is the hypotenuse, as it is the side opposite vertex B where the right angle lies."* while `options` marks **B** (`{"label": "B", "text": "AC, because it is the side opposite the right angle at B.", "is_correct": true}`). A stale label left behind by STEP 6's relabelling — the same family as ARV-D-092 but in a field `repair_option_reveals.py` does not touch, and it prints a wrong letter straight at the teacher. (iii) **Canonical `Q-A-1`'s diagnostics are mapped to the wrong labels** (a cyclic shift the `option_reveals_conform` repair did not correct — that pass declares only `dropped_correct_entry: ["B"]`, no `moves`): key `A` reads *"has not read all three measurements carefully, or does not know the definition of **scalene**"* but option A is *"Equilateral, because two sides each measure 5 cm"*; key `C` reads *"conflates 'two sides equal 5 cm' with 'all three sides are equal', confusing isosceles with **equilateral**"* but option C is *"Right-angled, because 5² + 5² equals 6²"*; key `D` reads *"applying an unrelated numerical check (**Pythagoras**)"* but option D is *"Scalene, because all three sides have different lengths"*. p07's single MCQ is correctly mapped (`B`→right-angled claim, `C`→isosceles/equilateral confusion, `D`→scalene), so this is canonical-only. `expected_answer` is words-and-numbers with no figure throughout; `method_one_line` is one sentence on all 19 items; no C-codes anywhere in `teacher_guide`. |
| **A 7** | `visual_stimulus` only "" , a pipe-delimited table, or `number_line:`; SVG / ASCII art / image markup / italic figure specs PROHIBITED | **PASS** | **PASS** | `"visual_stimulus": ""` on **all 12** canonical items and **all 7** p07 items. `grep -c '<svg'` → 0 in both; no pipe character in any stimulus field; no `number_line:` token; no base64 or image reference. Consistent with the rule's own guidance that "" is the default for geometry — the figures are reached through the `exercise` companion (e.g. `Q-C-5.exercise = "Figure it Out, section 7.4 p.168"`) or by textbook reference. |
| **A 8** | `anchor_id` never appears in teacher-facing prose; the exercise renders `book_ref` + `description` only | **PASS** | **PASS** | Regex `\b(A\|E\|WE)-\d+\b` over every `prompt`, `expected_answer`, `method_one_line`, `what_each_option_reveals` value, `inclusivity` clause and `exercise.description` in both files → **0 hits**. Prompts locate the textbook by book language where they need to (`Q-C-1.inclusivity.support`: *"Refer to the definition of an arc in **section 7.1** of your textbook"*), never by internal id. |
| **A 9** | `exercise.book_ref` / `exercise.description` sourced verbatim from `handoff.anchor_book_ref` / `anchor_description`; empty when no anchor | **PASS** | **PASS** | Byte-compared item-by-item against the handoff and against the summary's enumeration: **0 mismatches in either file**. Example, canonical `Q-B-1`: `exercise.book_ref = "Math Talk, section 7.2 p.151"` and `description = "Attempt to construct triangles with sidelengths 3 cm, 4 cm, 8 cm and 2 cm, 3 cm, 6 cm. Find more sets of lengths for which a triangle is impossible, and identify a pattern."` — identical to `section_b.goals[0]` and to `E-4` in the summary. Neither file has an empty-anchor item. *Caveat carried from LP 11, not scored here:* canonical `Q-C-7`'s companion faithfully copies `E-30`, an anchor the LP should not have selected for that unit, and p07 `Q-C-4`'s copies `E-25` where `E-24` was due. |
| **A 10** | `question_type` from `{MCQ, SCR, ECR, NUM}`; MCQ = exactly 4 options with exactly one `is_correct`, 3 distractor diagnostic keys, no option referring to another **by label** | **PASS** | **PASS** | Canonical types: `MCQ, MCQ, SCR, SCR, ECR, NUM, NUM, NUM, NUM, NUM, SCR, SCR`; p07: `MCQ, SCR, ECR, NUM, NUM, NUM, SCR` — all inside the enum, and all inside the per-goal default map (`recall → MCQ`, `reason → SCR/ECR`, `apply → NUM/SCR`) on **every** item of both files. All three MCQs carry exactly 4 options with exactly one `is_correct: true` and exactly 3 `what_each_option_reveals` keys. **No by-label option** — no "both A and B", no "none/all of the above"; the nearest string, canonical `Q-A-2` option C *"All three sides, because any side can be the hypotenuse depending on how the triangle is oriented"*, refers to the triangle's sides, not to other options, and survives reordering intact. SCR/ECR/NUM items carry `options: []` and `what_each_option_reveals: {}` as required. |
| **ANCHORING** | `section_ref` (+ `section_title`) copied verbatim from the handoff in the LP's own vocabulary; **`period_ref` / `period_number` / any unit number MUST NOT be emitted** | **PASS** | **PASS** | `grep -c period_ref` → **0** in both files; no item carries a unit number in any form. `section_ref` values are the LP's own vocabulary (`"section 7.5"`, not `"§7.5"`) and match the handoff entry they came from, one for one; `section_title` matches the summary's `sections[i].title` verbatim on all 19 items (e.g. `"Construction of Triangles When Some Sides and Angles are Given"`). *Recorded, not scored here (it is a C9 matter, and already closed in code):* because canonical unit 12 lists all five sections, "the item anchors at the LAST unit teaching that section" would have collapsed all twelve items onto unit 12 — `aruvi_core/genon/carriers.py::items_by_period_field` now excludes `is_synthesis(p)` from the index, with the comment naming ch 7 unit 12 as where it was found. |
| **ASSESSMENT JSON SCHEMA** | Envelope `chapter_number`, `chapter_title`, `stage`; per-item field presence and permitted values | **FAIL** | **FAIL** | Item level is complete and correctly typed on all 19 items: `id, section_ref, section_title, goal, question_type, prompt, visual_stimulus, options[{label,text,is_correct}], teacher_guide{expected_answer, method_one_line, what_each_option_reveals, inclusivity{support,challenge}}, exercise{book_ref, description}, verified`. `goal` only `recall|reason|apply`; `section_code` only `A|B|C` with the mandated `section_title` strings; `note` present as `""`. **FAIL is narrow and shared: the envelope omits `"stage"`** (`grep -c '"stage"'` → 0 in both, and in the raw authored output `ch_07_20260810_184152_raw.txt` as well); `chapter_number`/`chapter_title` do survive, on the saved-plan envelope. Same platform-wide shape as LP 7 — the authored JSON's envelope is the pipeline's, not the constitution's. |

---

## Founder-facing notes

1. **Rule vs brief collision (LP 1 / LP schema).** The v2.0 architecture mandates a closing
   whole-chapter synthesis unit on the STANDARD canonical; LP v3.6 Rule 1 caps a unit at "one or at
   most two adjacent sections" and forbids re-anchoring a completed section, and the schema says
   `textbook_segments // 1–2 entries`. Unit 12 cannot satisfy both. The generation did the sensible
   thing and the platform has already adapted around it in code (`carriers.is_synthesis`). This
   reads as an **S4-style P1 amendment** (a carve-out naming the synthesis unit), not a defect in
   the plan — but until the carve-out exists, C3 has to score it FAIL.
2. **Numeric limits are again the thing live generation disproves** — exactly the lesson S4 wrote
   down at 2026-08-09. Two caps are breached wholesale rather than occasionally: LP Rule 10's
   **10-word** bracketed description (26/26 and 15/20) and assessment Rule 6's **25-word**
   `inclusivity` pair (19/19). A 10-word cap cannot carry an NCERT "Figure it Out" stem that lists
   seven lettered cases; a 25-word support+challenge pair cannot carry a differentiated maths
   extension. Both look like rule-side numbers to re-cut, but that is a founder call, and the
   defects stand until it is made.
3. **Two things a repair pass missed** and would be cheap to close in place: canonical `Q-A-2`'s
   `expected_answer` leading with the stale letter **"C —"** against a correct option labelled
   **B**, and canonical `Q-A-1`'s three diagnostics sitting under the wrong labels. Both are the
   ARV-D-092 family; `repair_option_reveals.py` fixed the key SET but not the key→text mapping on
   item 1, and does not look at `expected_answer` at all.
4. **`Q-C-7` (canonical) is the weakest artefact in the pair** and it traces to one bad handoff
   entry: LP 11 anchored unit 12 to `section 7.5` with `E-30`, so the assessment inherited a
   `recall`-only section under an `apply` goal, copied the anchor's spider-and-box, and answered it
   with Pythagoras — a method this chapter never teaches. One entry, four rules touched (LP 11,
   A 3 twice over, and the goal/section mismatch).
5. **Still owed for C3:** the maths determinate-answer worksheet
   (`genon/extract_determinate.py mathematics vii 7`, filled and run over **all three** installed
   canonicals — the top, `p07` and `p10`). My re-derivations above were done with
   `method_one_line` visible and so do not discharge that check.
6. **Already recorded, not re-opened here:** ARV-D-089 (p10's non-contiguous closing unit — `p10`
   is not one of this pair), ARV-D-092 (option-reveals mis-mapping — repaired, with the two
   residues at note 3), ARV-D-093 (label-in-prose on maths IX).
