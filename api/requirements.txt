# Aruvi API (FastAPI) dependencies — the backend bridge to aruvi_core.
fastapi==0.128.8
uvicorn==0.39.0
httpx==0.28.1          # used by the TestClient in tests
xhtml2pdf==0.2.17      # allocation-report PDF export (HTML→PDF, pure-Python, NO system libs)
python-docx==1.2.0     # allocation-report DOCX export (pure-Python, cloud-safe)
