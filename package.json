{
  "name": "aruvi-monorepo",
  "private": true,
  "description": "npm workspaces root — web/ (Next.js) and packages/shared (client logic shared with the Expo phone app, Track D). The Python API is not part of this; see pyproject.toml.",
  "workspaces": [
    "packages/*",
    "web"
  ],
  "scripts": {
    "web": "npm --prefix web run dev",
    "build:web": "npm --prefix web run build",
    "test:shared": "npm --workspace @aruvi/shared test"
  }
}
